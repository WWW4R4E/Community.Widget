<?xml version="1.0" encoding="utf-8" ?>
<Window x:Class="Community.Widget.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:contracts="using:MyLittleWidget.Contracts"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="using:Community.Widget"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="Community.Widget"
        mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid x:Name="PadRootGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition />
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid x:Name="PadPresenter"
              Grid.Row="2"
              Grid.Column="1"
              Background="Transparent"
              BorderBrush="{ThemeResource AccentTextFillColorTertiaryBrush}"
              BorderThickness="2"
              CornerRadius="0">
            <Grid x:Name="Pad" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
    </Grid>
</Window>
